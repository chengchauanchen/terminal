<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- 填充颜色 -->
    <solid android:color="@color/change_group_bg" />

    <!-- 矩形的圆角半径 -->
    <corners
        android:bottomLeftRadius="@dimen/dp_15"
        android:bottomRightRadius="@dimen/dp_15"
        android:topLeftRadius="@dimen/dp_15"
        android:topRightRadius="@dimen/dp_15" />
    <!-- 线的宽度，颜色灰色 -->
    <stroke
        android:width="0.7dp"
        android:color="@color/change_group_line_bg" >
    </stroke>

</shape>