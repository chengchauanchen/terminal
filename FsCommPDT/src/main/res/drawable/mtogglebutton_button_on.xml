<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">

    <!-- 填充颜色 -->
    <solid android:color="@color/mtoggle_on_button" />

    <!-- 线的宽度，颜色灰色 -->
    <stroke
        android:width="0dp"
        android:color="@color/mtoggle_on_button"></stroke>

    <size
        android:width="25dp"
        android:height="25dp" />

</shape>
